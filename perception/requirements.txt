# Core -- needed for costmap.py (classical pipeline)
numpy
opencv-python

# Optional -- YOLO obstacle detector (costmap.py --use-yolo)
# ultralytics

# Optional -- learned segmentation (twinLiteNetTest.py).
# Also requires the external TwinLiteNetPlus repo + pretrained weights;
# see the header of twinLiteNetTest.py for setup.
# torch
# torchvision
# pyyaml
# tqdm
